#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main - generate random valid password
 *
 * Return: Always 0.
 */
int main(void)
{
int index = 0, diff1, diff2;
char password[90];

srand(time(0)));

while (sum < 2772)
{
password[index] = 33 + rand() % 94;
sum += password[index++];
}

password[index] = '\0';

if (sum != 2772)
{
diff1 = (sum - 2772) p
[B
